name: confwebsite2021
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  flutter_svg: ^0.22.0
  transparent_image: ^2.0.0
  flutter_svg_provider: ^0.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: ^1.11.0+2
  url_launcher: ^6.0.2
  build_runner:
  flutter_gen_runner:

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/
    - assets/photo/